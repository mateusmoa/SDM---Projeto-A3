# Arquivo de exemplo para variáveis de ambiente
# Renomeie este arquivo para .env e preencha com seus valores

# Configurações do Banco de Dados PostgreSQL
POSTGRES_USER=unitask_user
POSTGRES_PASSWORD=sua_senha_segura_aqui
POSTGRES_DB=unitask_db

# Chave Secreta da Aplicação Flask (importante para sessões, etc.)
# Gere uma chave segura, por exemplo, com: python -c 'import secrets; print(secrets.token_hex(16))'
SECRET_KEY=sua_chave_secreta_flask_aqui

# A DATABASE_URL será construída automaticamente no docker-compose.yml
# Não precisa definir DATABASE_URL aqui se estiver usando o docker-compose fornecido

